// Este archivo se reescribe en tiempo de arranque por el contenedor Docker.
// En desarrollo (vite dev) se ignora y se usan las variables VITE_* o los defaults.
window.APP_ENV = {
  AUTH_API_URL: '',
  PARQUEADERO_API_URL: ''
};
