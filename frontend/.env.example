# Solo se usan en desarrollo (vite dev). En producción/Docker se ignoran y
# se reciben las URLs vía AUTH_API_URL y PARQUEADERO_API_URL en runtime.
VITE_AUTH_API_URL=http://localhost:4001
VITE_PARQUEADERO_API_URL=http://localhost:4002
