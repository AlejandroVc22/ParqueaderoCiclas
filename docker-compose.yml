services:
  # =========================================
  # PostgreSQL - Base de datos del Auth
  # =========================================
  postgres:
    image: postgres:16-alpine
    container_name: cp_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-auth_db}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - cp_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-auth_db}"]
      interval: 10s
      timeout: 5s
      retries: 10

  # =========================================
  # MySQL - Base de datos del Parqueadero
  # =========================================
  mysql:
    image: mysql:8.0
    container_name: cp_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-parqueadero_db}
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - cp_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p${MYSQL_ROOT_PASSWORD:-root}"]
      interval: 10s
      timeout: 5s
      retries: 15

  # =========================================
  # Microservicio Auth (Node + Postgres)
  # =========================================
  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    container_name: cp_auth_service
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 4001
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${POSTGRES_DB:-auth_db}
      DB_USER: ${POSTGRES_USER:-postgres}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      JWT_SECRET: ${JWT_SECRET:-ciclo-parqueadero-super-secret-key-change-in-production}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-24h}
      ADMIN_NAME: ${ADMIN_NAME:-Administrador}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@cicloparqueadero.com}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-Admin123!}
    ports:
      - "4001:4001"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - cp_network

  # =========================================
  # Microservicio Parqueadero (Node + MySQL)
  # =========================================
  parqueadero-service:
    build:
      context: ./parqueadero-service
      dockerfile: Dockerfile
    container_name: cp_parqueadero_service
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 4002
      DB_HOST: mysql
      DB_PORT: 3306
      DB_NAME: ${MYSQL_DATABASE:-parqueadero_db}
      DB_USER: root
      DB_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      JWT_SECRET: ${JWT_SECRET:-ciclo-parqueadero-super-secret-key-change-in-production}
    ports:
      - "4002:4002"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - cp_network

  # =========================================
  # Frontend (React + Vite + Nginx)
  # =========================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: cp_frontend
    restart: unless-stopped
    environment:
      PORT: 80
      AUTH_API_URL: ${AUTH_API_URL:-http://localhost:4001}
      PARQUEADERO_API_URL: ${PARQUEADERO_API_URL:-http://localhost:4002}
    ports:
      - "5173:80"
    depends_on:
      - auth-service
      - parqueadero-service
    networks:
      - cp_network

networks:
  cp_network:
    driver: bridge

volumes:
  postgres_data:
  mysql_data:
